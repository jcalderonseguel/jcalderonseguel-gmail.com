const prefix = 'CREATE_USER_';

const ids = {
  BUTTON_REGISTER: `${prefix}BUTTON_REGISTER`,
  INPUT_NAME: `${prefix}INPUT_NAME`,
  INPUT_EMAIL: `${prefix}INPUT_EMAIL`,
  INPUT_PASSWORD: `${prefix}BUTTON_PASSWORD`,
  FORM_CREATE_USER: `${prefix}FORM`,

};

export default ids;
